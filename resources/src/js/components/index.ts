/**
 * Alpine component registrations. Each module attaches its `Alpine.data(...)`
 * call to the `alpine:init` event, so importing this index is enough to make
 * every factory available to blade templates.
 */
import './auto-click';
import './autocomplete-suggestions';
import './category-list';
import './checkout-fulfillment';
import './cookie-banner';
import './quantity-option';
import './slider';
